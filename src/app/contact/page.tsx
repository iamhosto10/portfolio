import ContactFormSection from "@/components/ContactFormSection/ContactFormSection";

const page = () => {
  return (
    <main className="min-h-full bg-background/50">
      <ContactFormSection />
    </main>
  );
};

export default page;
